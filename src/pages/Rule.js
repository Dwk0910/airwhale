import TopBar from './../templates/TopBar';

function Rule() {
    document.title = "에어웨일 - 규정";

    return (
        <div className={"content"}>
            <TopBar/>
        </div>
    );
}

export default Rule;